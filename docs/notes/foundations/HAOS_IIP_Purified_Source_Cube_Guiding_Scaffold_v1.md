# HAOS-IIP Purified Source Cube Guiding Scaffold v1

This note is a guiding interpretive mnemonic and unification scaffold only. It does not modify HAOS core, frozen telemetry, numerical thresholds, or any existing `PASS` / `OPEN` / `WATCH` status. It compresses the already-frozen scalar-carrier ladder (`51.3`-`52.5`) and bridge state through `53.4` into a compact structural mnemonic.

## 1. Restated Ontology

Under the root-note guiding principle, HAOS-IIP can be read as a frozen branch-local cochain-Laplacian hierarchy whose deepest persistent scalar-carrier baseline acts as an invariant carrier, while later recoverable structures appear as bounded harmonic partials. The purified Source Cube is only a mnemonic scaffold for that reading: its core corresponds to the stable scalar-carrier window (`51.3` robustness, `51.4` positive near-isotropic metric field, `52.1` recoverability probe), and its outer axes organize later recoverable closures and boundaries, including weak localized excitation `PASS` / strong localized-bump `OPEN` (`53.2`), shell-native power-law `PASS` / raw local-gradient power-law `OPEN` (`53.3`), and seed-universal mild-disorder flux `PASS` with overall bridge state `PASS 12 / OPEN 3 / WATCH 0` (`53.4`). This remains bounded feasibility diagnostics only, with no claim about real spacetime, GR, QFT, or continuum ontology.

## 2. Purified Source Cube Definition

The purified Source Cube is a guiding interpretive mnemonic and unification scaffold only. It is not a mystical object, physical cube, or replacement for the frozen derivation ladder. Operationally, it is a compact addressability map: the cube core denotes the invariant carrier, while faces or axes denote distinct harmonic readout directions through which already-frozen observables can be tuned, filtered, or pruned by external reconstruction choices such as shell-native filters or delayed fit windows.

The Source Cube functions solely as a visual and conceptual mnemonic scaffold. It organizes the already-achieved HAOS-IIP emergence ladder without introducing new operators, claims, or modifications to the frozen physics bridge state (`PASS 12 / OPEN 3 / WATCH 0` as of `53.4`).

Key faces or axes:

- `Core axis: invariant carrier`
  Maps to the scalar-carrier baseline closed by `51.3`, `51.4`, and the persistence boundary made explicit in `52.1`.
- `Propagation axis: branch transmission`
  Maps to the early HAOS ladder propagation bands and the low-mode transport organization that precedes the later scalar-carrier bridge.
- `Ordering axis: temporal recovery`
  Maps to temporal ordering and causal stabilization in the `T1-T8` ladder, treated as recoverable phase ordering rather than ontology.
- `Distance axis: proto-geometry`
  Maps to proto-distance surrogates and later metric-style scalar reads culminating in the bounded local metric field.
- `Transport axis: constitutive closure`
  Maps to clean current-closure and smooth inhomogeneity tracking through `52.4` and `52.5`.
- `Excitation axis: localized partials`
  Maps to `53.2`, where weak localized excitations pass after delayed fit-window pruning while stronger amplitudes remain open.
- `Law-recovery axis: harmonic filtering`
  Maps to shell-native inverse-square / power-law recovery through `53.3`.
- `Disorder-robustness axis: bounded seed stability`
  Maps to seed-universal disorder-native flux closure through `53.4`.

### Expanded Axes Mappings

This expanded mapping uses the purified Source Cube as a guiding interpretive mnemonic and unification scaffold only. It does not alter the frozen scalar-carrier ladder, bridge thresholds, or bridge statuses through `53.4`.

- `Core: invariant carrier / root note`
  Maps to the scalar-carrier baseline already supported by `51.3`, `51.4`, and `52.1`.
  Harmonic meaning: minimal persistent carrier, near-isotropic and low-drift, from which later recoverable content is read.
- `Axis 1: propagation`
  Maps to branch transmission, early propagation bands, and low-mode transport organization in the frozen derivation ladder.
  Harmonic meaning: first coherent spread of the carrier into bounded wavefront structure.
- `Axis 2: ordering`
  Maps to temporal ordering, causal recovery, and the `T1-T8` / `F1-F4` derivation ladder.
  Harmonic meaning: recoverable phase-locking that turns raw interaction into ordered partials.
- `Axis 3: distance`
  Maps to proto-distance surrogates, geometric integrity, and the bounded local metric-field read.
  Harmonic meaning: stabilized distance-like structure emerging from bounded harmonic interference.
- `Axis 4: transport`
  Maps to clean-line current closure `52.4` and smooth-inhomogeneity tracking `52.5`.
  Harmonic meaning: constitutive flow and transport closure as higher-order carrier organization.
- `Axis 5: excitation`
  Maps to `53.2`, where weak localized excitations pass and stronger localized bumps remain open.
  Harmonic meaning: low-amplitude recoverable partials versus amplitude regimes that exceed the current coherence budget.
- `Axis 6: law-recovery`
  Maps to `53.3`, where shell-native power-law and inverse-square recovery pass while raw local-gradient scaling remains open.
  Harmonic meaning: shell-native harmonic filtering that isolates a clean partial from raw residual structure.
- `Axis 7: disorder-robustness`
  Maps to `53.4`, where seed-universal disorder-native flux closes at `3/3` on the tested mild-disorder window.
  Harmonic meaning: higher-order meta-stability in which the recovered partial remains phase-locked to the carrier under bounded disorder.

Operational pruning and tuning remain external:

- delayed fit windows act as bounded pruning operations, not threshold changes, in `53.2` (`[0.010, 0.026]`) and `53.4` (`[0.006, 0.024]`);
- shell-native reconstruction acts as law-aware harmonic filtering in `53.3`;
- the remaining `OPEN` rows are natural boundary markers of the current coherence budget, not hidden positive evidence.

### Pruning Mechanisms

This pruning account uses the purified Source Cube as a guiding interpretive mnemonic and unification scaffold only. It does not introduce new operators, alter telemetry, or modify the frozen bridge state through `53.4`.

Operational definition:

- pruning is the selective damping or exclusion of non-recoverable harmonic content so the hierarchy remains phase-locked to the invariant carrier;
- pruning is external post-processing only, not arbitrary deletion and not threshold relaxation;
- all canonical `PASS` / `OPEN` results remain defined by the frozen scalar-carrier thresholds and bridge rows.

Primary mechanisms:

- `Transient exclusion via delayed fit windows`
  Used in `53.2` and `53.4` to exclude source-core or late asymptotic contamination while leaving thresholds fixed. In Source Cube terms this prunes unstable content on the excitation and disorder-robustness axes.
- `Shell-native / law-aware reconstruction`
  Used in `53.3` to recover a clean shell-level power-law / inverse-square partial while the raw local-gradient read remains open. In Source Cube terms this is harmonic filtering on the law-recovery axis.
- `Persistence thresholding`
  Structures that fail structural retention, refinement stability, or safety-margin checks are not promoted. In Source Cube terms this preserves only content that remains trace-connected to the cube core across the ordering and distance axes.
- `Amplitude / coherence budget thresholding`
  The weak versus stress split in `53.2` is treated as a bounded amplitude budget. In Source Cube terms, low-amplitude localized partials remain recoverable while stronger excitations are pruned or left at the boundary.
- `Seed-universal stability checks`
  The `3/3` closure in `53.4` is treated as a robustness filter across bounded seed variation. In Source Cube terms, a recovered partial counts as stable only if it remains coherent across the tested disorder seeds after transient pruning.

Interpretive boundary:

- in signal-processing terms, pruning behaves like time-windowed or reconstruction-selective filtering;
- in discrete geometric or cochain language, pruning behaves like restricting readout to sub-intervals or reconstruction layers that preserve recoverable structure relative to the carrier;
- in all cases, pruning stays external to `haos_core`, frozen operators, and canonical telemetry.

## 3. Updated Layered Architecture

This layered architecture uses the purified Source Cube as a guiding interpretive mnemonic and unification scaffold only. All frozen numerics, thresholds, and `PASS` / `OPEN` / `WATCH` statuses remain unchanged.

- `Layer 0: Root Carrier (Cube Core)`
  The cube core names the deepest persistent carrier already evidenced by the bounded scalar-carrier geometry window: common `3D` kernel-graph family, low drift, positive metric read, and survival under perturbative probing.
- `Layer 1: Primary Harmonics`
  The first outer cube structure corresponds to the branch-local cochain-Laplacian hierarchy itself: spectral bands, low-mode organization, and the HAOS-to-harmonic ladder (`T1-T8`, `F1-F4`) viewed as discrete harmonic generation channels.
- `Layer 2: Recoverable Structures`
  Faces or axes become populated only when a structure remains trace-connected to the cube core under perturbation. This includes bounded transport closure (`52.4`, `52.5`), weak localized excitation `PASS` (`53.2`), shell-native harmonic-law recovery `PASS` (`53.3`), and seed-universal disorder-native flux `PASS` (`53.4`).
- `Layer 3: Pruned Or Boundary Structures`
  Cube regions that fail recoverability are not promoted. In current frozen form, these are the raw inverse-square residual `OPEN`, raw local-gradient power-law scaling `OPEN`, and strong localized-bump boundary `OPEN`.
- `Layer 4: Emergent Regimes`
  Physics-facing language enters only as external bridge proxy language. The cube organizes those proxy reads as addressable regimes, not as physical ontology or continuum proof.

## 4. Revised Key Protocols

These protocols use the purified Source Cube as a guiding interpretive mnemonic and unification scaffold only. They do not alter canonical outputs.

- `Persistence probes`
  Read each candidate as a cube-axis persistence test: does the structure remain phase-trace connected to the cube core under the existing frozen perturbations and refinement scans?
- `Pruning rules`
  Treat delayed fit windows as bounded transient-pruning operations, not as rescue devices. In current frozen use this matches `53.2` (`[0.010, 0.026]`) and `53.4` (`[0.006, 0.024]`), where pruning removes early or late transient contamination while thresholds remain fixed.
- `Physics-bridge evaluation`
  Treat shell-native reconstruction as cube-axis harmonic filtering. The clean partial is the shell-native `PASS`; the raw local-gradient residual remains an `OPEN` boundary and must stay visible beside it.
- `Telemetry relation`
  Existing telemetry remains authoritative. The cube may only annotate it with sidecar labels such as `core-coherent`, `boundary`, or `pruned`, derived from existing status and margin structure.
- `Addressability / tuning`
  External selection of fit windows, shell-native filters, or disorder readout windows is interpreted as cube tuning only when it remains reproducible, leaves thresholds fixed, and operates strictly on frozen artifacts.

## 5. Implementation Roadmap

This roadmap treats the purified Source Cube as a guiding interpretive mnemonic and unification scaffold only.

- `TERMINOLOGY.md`
  Add non-ontic entries for `Source Cube`, `cube core`, and `cube tuning`, explicitly marked as external language only.
- `README.md`
  Add a single pointer to this note with wording that it is a mnemonic scaffold and leaves the frozen stack unchanged.
- `docs/notes/foundations/HAOS_IIP_Root_Note_Guiding_Principle_v1.md`
  Add a one-line cross-reference if later desired, keeping root-note and Source Cube language visibly subordinate to the same external layer.
- `docs/notes/foundations/HAOS_IIP_Physics_Bridge_53_0_v1.md`
  Leave canonical bridge rows unchanged; at most add a cross-reference sentence clarifying that any Source Cube reading is post-processing only.
- `experiments/physics_bridge/physics_observables.py`
  Leave canonical outputs unchanged. If extended later, do it in a separate sidecar script such as `source_cube_observables.py`.

Suggested visual:

- `docs/media/source_cube_scalar_carrier_map.svg`
  A simple diagram with a center core node for the invariant carrier, seven outer faces or axes for propagation, ordering, proto-geometry, transport, excitation, law-recovery, and disorder-robustness, plus red boundary markers on the three current `OPEN` bridge rows.

Suggested external metric:

- `cube_root_alignment_margin`
  A sidecar composite margin defined over carrier stability, drift-to-threshold distance, and metric-tracking compatibility. It should be computed only from frozen outputs and never replace the canonical `PASS` / `OPEN` rows.

## 6. Minimal Verification Protocol

This protocol uses the purified Source Cube as a guiding interpretive mnemonic and unification scaffold only.

1. Read the cube core first: confirm the stable carrier window from `51.3`, `51.4`, and the persistence boundary exposed in `52.1`.
2. Read one cube axis at a time: transport, excitation, law recovery, or disorder; do not merge axes before checking the frozen bridge row.
3. If the row is `PASS`, verify that the partial remains low-drift and threshold-respecting under the exact frozen reconstruction window.
4. If the row is `OPEN`, mark that cube face as boundary or pruned content rather than hidden positive evidence.
5. Re-run `python3 examples/quick_reproduce.py --physics-observables-only` before promoting any cube-language summary.

The Source Cube serves as a purified bridge between HAT intuition and HAOS-IIP numerics, while remaining fully subordinate to the frozen HAOS diagnostic stack.

The safe conclusion is narrow: the purified Source Cube can serve as a clean mnemonic scaffold for the already-frozen HAOS-IIP ladder, but it must remain external, reproducible, and subordinate to the existing numerical authority.
