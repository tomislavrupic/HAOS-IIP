# HAOS-IIP Next-Rung Plan - 2026-07-11

Current frontier: `RUNG 2 SUPPORTED / RUNG 3 NEGATIVE_RESULT`.

## Priority 1 - EL-R3-THEORY-REVISION-01

RT-01, RT-02, and RP-01 are now frozen. RP-01 demonstrates that local parity can
correct the declared relational code inside its radius, yet the independent
function remains unrecovered. This rules out another decoder or gain pass over
the same order-bit alphabet.

Before another implementation, identify an independently motivated mesoscopic
restorative variable that carries amplitude, phase, transition, or equivalent
function-relevant information without uniquely reconstructing the continuous
pre-state. The theory revision must quantify its information budget, locality,
failure basin, and destructive intervention before choosing a mechanism.

No implementation is authorized until that precommitment exists. If no such
bounded representation can be justified independently of the evaluation
function, Rung 3 remains a durable negative result.

## Priority 2 - EL-R4-OC-02 Intervention-Native Closure

Use state trajectories rather than compressed event chains. Freeze internal
interaction variables, external forcing variables, equal information budgets,
interventions, and holdout regimes before fitting.

Required controls:

- internal-edge destruction with external forcing preserved;
- external-forcing shuffle with internal topology preserved;
- equal-dimensional random state projection;
- passive autoregressive baseline;
- target-label leakage positive control.

Stop if internal prediction does not beat the best external/matched-compression
baseline with a run-level interval excluding zero, or if the advantage survives
internal-edge destruction.

Dependency gate: Rung 3 must first be supported.

## Priority 3 - EL-R5-CSR-01 Cross-Scale Recovery

Dependency gate: a Rung 4 mechanism must first survive intervention. Then test
ordered local, mesoscopic, and global recovery with scale-shuffled,
cross-scale-disrupted, passive-decay, and random-recovery controls.

Do not call refinement stability cross-scale organization without direct
dependency-transfer evidence.

## Deferred - EOT-02 Artifact-Derived Effective Dynamics

Dependency gate: enough independent refinements for development, calibration,
and untouched holdout. Predeclare allowed terms and compare against conventional
Laplacian, spectral, and equal-compression baselines.

EOT-01 remains a synthetic calibration. Its generated term hierarchy is not
evidence that the same terms occur in frozen HAOS artifacts.
