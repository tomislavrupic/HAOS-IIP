# Papers

This folder is the publication front door for the frozen-architecture program.

Phase I should be read as one bundled result with one primary citation target:

1. `18.1` is the capstone paper for arXiv / journal submission.
2. `00.1` through `17.1` are the sequential release notes that support, justify, and document that capstone.

## 1. Capstone paper

1. Primary PDF: `pdf_releases/18.1 Limits of Frozen Pre-Geometry on a Kernel-Induced Cochain Operator Architecture.pdf`
2. Source: `limits_of_frozen_pre_geometry_on_a_kernel_induced_cochain_operator_architecture.tex`
3. External reference rule: cite `18.1` as the Phase I paper.

The capstone compresses the full frozen arc into one readable, citable boundary statement:

> Frozen pre-geometry can organize, but it does not bind.

## 2. Living archive

The capstone is supported by a permanent archive layer:

1. `pdf_releases/INDEX.md` for the full release stack
2. `experiments/pre_geometry_atlas/` for the staged atlas notes
3. `numerics/simulations/` for the numerical solvers
4. `plots/` and `data/` for generated outputs
5. Zenodo archive DOI: `10.5281/zenodo.18938323`

The release logic is:

1. the paper carries the argument
2. the repository carries the mechanics
3. the Zenodo DOI freezes the archive state

## 3. Compression map for Phase I

The release stack already exists as `00.1` through `18.1`. For public delivery, compress it into four numbered blocks:

### 3.1 Part I. Architecture

1. `00.1` Operator architecture
2. `01.1` kernel-induced operator architecture and transverse band
3. `02.1` continuum reconstruction and scalar-transverse coupling
4. `03.1` Dirac-Kaehler factorization
5. `04.1` gauge-like diagnostics

### 3.2 Part II. Transport and morphology

1. `05.1` wave-packet propagation and constraint stability
2. `06.1` morphology survey
3. `07.1` perturbation resilience
4. `08.1` transition structure
5. `09.1` unified pre-geometric atlas
6. `10.1` collective interaction morphology

### 3.3 Part III. Collective organization

1. `11.1` coarse-grained collective envelope structure
2. `12.1` resolution-interaction coupling
3. `13.1` effective binding existence tests

### 3.4 Part IV. Boundary limit

1. `14.1` emergent relational geometry probe
2. `15.1` relational feedback dynamics probe
3. `16.1` emergent phase synchronization probe
4. `17.1` emergent memory / retarded interaction probe
5. `18.1` capstone compression of the frozen limit

## 4. Suggested public release order

1. Release `18.1` as the capstone paper.
2. Point the paper to the Zenodo DOI and the repository release index.
3. Keep `00.1` through `17.1` in `papers/pdf_releases/` as technical appendices / experiment notes, not as separate journal targets.
4. Present Phase II separately as the onset test for substrate back-reaction.

## 5. Website front door

Use this short summary anywhere the project needs a public-facing entry point:

> Phase I maps the boundary of a frozen kernel-induced cochain architecture. The system supports morphology, collective organization, and localized ordering, but weak clean extensions do not produce robust binding, geometry-like back-reaction, or stable timing law. Phase II begins where Phase I closes: the interaction kernel becomes dynamical.
