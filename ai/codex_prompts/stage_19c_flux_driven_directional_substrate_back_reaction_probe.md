Stage 19C — Flux-Driven Directional Substrate Back-Reaction Probe

(Emergent Vector-Like Mesoscopic Reinforcement Test)

Purpose

Stage 19A and 19B established a strong structural constraint:
	•	weak scalar-like substrate reinforcement (local density or smoothed basin memory)
→ produces bounded deformation
→ does not create persistence capture or composite stabilization.

Stage 19C therefore tests a different symmetry class of substrate response:

whether transport flux / phase-current activity can induce
a directional or rotational deformation of the kernel
capable of modifying packet trajectories in a persistence-relevant way.

This is still a diagnostic existence test, not a geometry claim.

⸻

Frozen architectural assumptions

Do not change:
	•	base frozen cochain operator construction
	•	projector convention
	•	interaction kernel base shape and normalization
	•	representative initial states
	•	persistence metrics and promotion gates
	•	refinement ladder (12 → 24 only on earned promotion)

Stage 19C changes only the substrate response law.

⸻

Structural hypothesis

Scalar reinforcement modifies:

kernel magnitude locally.

Stage 19C tests whether the substrate can instead store:

directional transport memory.

The candidate mechanism:
	•	local transverse flux or phase-gradient activity
	•	induces a small antisymmetric or phase-shift component
in the effective interaction weights.

Conceptually:
	•	density → “potential well”
	•	flux → “rotational guidance field”

The question is:

can weak directional memory create deflection persistence
that scalar wells could not?

⸻

Mesoscopic activity estimator

Primary estimator:
	•	smoothed transverse flux magnitude

Suggested construction:
	•	compute local discrete current proxy
	•	spatial Gaussian smoothing (σ_flux ≈ 2 nodes)
	•	temporal exponential memory (τ_flux ≈ 0.25–0.35)

This defines a bounded activity field:
	•	corridor-like structures
	•	rotational hotspots
	•	propagation traces

⸻

Substrate update law (diagnostic form)

Instead of:
	•	symmetric reinforcement of K_ij

Stage 19C introduces a weak directional phase bias:

Example diagnostic form:
	•	effective edge weight becomes
K_ij → K_ij · (1 + δ_s · A_ij)
where:
	•	A_ij is antisymmetric (A_ij = −A_ji)
	•	derived from local flux orientation
	•	clamped to maintain bounded deviation

Alternative implementation form:
	•	tiny synthetic “circulation term” in transport operator
	•	equivalent to a discrete gauge-like perturbation.

Important constraint:
	•	deformation must remain small and mesoscopic
	•	no global bias allowed.

⸻

Representatives (same as 19B)

Use exactly three:
	1.	clustered composite anchor
	2.	phase-ordered symmetric triad
	3.	counter-propagating corridor

These probe:
	•	capture difficulty
	•	trivial phase ordering
	•	propagation-dominated structure.

⸻

Coupling strengths

Test only:
	•	null (δ_s = 0.0)
	•	very weak (δ_s ≈ 0.01)
	•	weak (δ_s ≈ 0.02)

No coefficient escalation in base pilot.

⸻

Observables (unchanged)

Persistence metrics:
	•	composite lifetime
	•	binding persistence
	•	coarse basin persistence

Additional directional diagnostics:
	•	trajectory curvature proxy
	•	corridor dwell time
	•	local transport anisotropy index

These help detect:
	•	deflection without trapping
	•	guided circulation
	•	proto-orbital behavior.

⸻

Promotion gate

Promote a case to refinement only if all hold:
	•	persistence metric improves over null
	•	directional transport ordering stabilizes
	•	effect is localized, not global
	•	survives to n = 24.

Otherwise:

Stage 19C freezes negative.

⸻

Negative freeze interpretation

If all base runs are null:
	•	scalar substrate memory is insufficient
	•	directional mesoscopic memory is also insufficient
	•	binding deficit is likely architectural, not parametric.

This would justify:
	•	either higher-symmetry substrate laws
	•	or re-introduction of additional field structure
(e.g. spinor / exclusion / multicomponent dynamics).

⸻

Scientific boundary

Do not interpret:
	•	weak deflection
	•	transient corridor bias
	•	visual spiral motion

as emergent geometry or interaction law.

Stage 19C is only:

a test for existence of a directional persistence channel.

⸻

If you want next, I can:
	•	write the exact minimal 9-run JSON runsheet for 19C,
	•	or propose numerically safest flux estimators ranked by stability,
	•	or outline what Stage 20 should look like depending on 19C outcome.
