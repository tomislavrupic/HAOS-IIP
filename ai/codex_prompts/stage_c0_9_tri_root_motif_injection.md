# Stage C0.9 Tri-Root and 3-Layer Multi-Motif Injection

HAOS-IIP Pre-Geometry Atlas - Combinatorial Kernel Line

Purpose

Stage C0.8 showed a partial positive: the weaker bounded-path kernel can open explicit adjacency-versus-kernel inconsistency, but protected irreversibility remains sparse. Stage C0.9 tests the minimal next escalation:

- keep the same weaker bounded-path kernel from C0.8
- replace the bi-root and two-layer motif ladder with tri-root and 3-layer motifs
- keep the protected topology-stability rule active
- alternate the weaker bounded-path kernel against adjacency once mismatch is live

The question is whether tri-root and 3-layer motif forcing can scale the hybrid C0.8 signal into broad, reproducible inconsistency-mediated protected irreversibility.

Frozen architecture

Keep fixed from C0.1-C0.8:

- graph size
- representative encounter families
- projected-transverse state sector
- shell-weight measurement stack
- no coordinate distance
- no Gaussian kernel
- no adaptive reinforcement law
- protected topology-stability constraint

Only change:

- continue with the bounded-path combinatorial kernel
- extend the motif family to tri-root and 3-layer constructions

Kernel definition

The weaker kernel remains purely combinatorial:

`w(h) = 1 / (h + 1)`

with finite support up to a fixed hop cap.

Allowed ingredients:

- adjacency
- bounded hop distance
- degree
- motif incidence
- shell counts

Not allowed:

- coordinates
- Euclidean distance
- Gaussian falloff
- embedding metrics

Forcing protocols

Run a 9-run matrix:

1. incidence-noise forcing
2. tri-root / 3-layer motif forcing
3. combined noise plus tri-root forcing

across:

1. clustered composite anchor
2. counter-propagating corridor
3. phase-ordered symmetric triad

Motif ladder

Cycle the multi-motif family through:

- tri-root triangle
- tri-root diamond
- tri-root hub micro-star
- three-layer stacked motif
- hybrid bi-root plus tri-root cross motif

Observables

Retain the C0.8 mismatch and inconsistency stack and add:

- tri_root_motif_correlation
- kernel_update_consistency_failure_count_under_tri_root
- protected_arrow_step_under_tri_root_inconsistency
- irreversibility_scaling_factor

Expected honest outcomes

Positive:

- consistency failures become positive in at least 6 of 9 runs
- protected arrow under tri-root inconsistency appears in at least 6 of 9 runs
- irreversibility scaling exceeds the C0.8 baseline of 2/9

This would support a broad inconsistency-mediated irreversibility claim on the no-distance branch.

Negative:

- failures remain sparse
- or protected arrow forcing stays below the 6 of 9 bar

This would define the final current boundary for the bounded-path family: inconsistency can be opened, but irreversibility does not scale reproducibly under the present control line.

Deliverables

- stamped JSON
- stamped CSV
- summary note
- Python runner
- trace and summary plots

Interpretation boundary

Do not claim emergent time or geometry as established.

At most claim:

tri-root and 3-layer motif forcing on the weaker bounded-path no-distance branch does or does not scale explicit kernel-update inconsistency into reproducible protected irreversibility.
