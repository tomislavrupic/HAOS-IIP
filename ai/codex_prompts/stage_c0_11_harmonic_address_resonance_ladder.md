# Stage C0.11 -- Harmonic Address Resonance Ladder

HAOS / IIP Pre-Metric Combinatorial Program

## Context

This stage stays inside the combinatorial no-distance regime:

- no coordinate geometry
- no metric kernel
- graph connectivity and interaction weights only
- Dirac-Kahler multi-component packet dynamics already active

C0.10 tested basic harmonic-address compatibility.
C0.11 asks whether discrete harmonic phase classes produce a reproducible resonance hierarchy rather than unstructured response.

## Goal

Determine whether discrete harmonic phase indexing generates:

- stratified collision textures
- persistent exchange families tied to phase class
- ordered resonance bands rather than continuous random variation

This stage is not a persistence-law claim.
It is a resonance-spectrum probe.

## Core mechanism

Assign each packet a discrete harmonic address:

`harmonic_address in {0, 1, 2, 3, 4}`

Use the phase-class map:

`phi = (2 pi / 5) * harmonic_address`

The address labels are purely relational phase indices.
They are not spatial coordinates and not frequency-distance claims.

## Fixed ingredients

Keep fixed:

- clustered two-packet encounter seed
- same DK operator family
- same packet width baseline
- same amplitude scale
- same graph size class

Vary only:

1. harmonic address pairing
2. mild degree-spectrum perturbation
3. optional motif injection

## Primary observables

Per run compute:

- collision topology label
  - `braid_like_exchange`
  - `smeared_transfer`
  - `localized_capture`
  - `dispersive_pass`
- `grade_exchange_coherence`
- `resonance_alignment_index`
- `topology_stability_flag`

## Ladder target

Look for:

- ordered phase bands
- special pairings
- monotonic or cyclic topology transitions across address difference

Do not interpret this stage as:

- a bound-state result
- a transport-channel claim
- a persistence promotion result

## Minimal run matrix

1. same-address baseline
2. address difference = 1
3. address difference = 2
4. address difference = 3
5. address difference = 4
6. randomized address pairing control
7. address difference = 2 with mild degree skew
8. address difference = 3 with motif injection
9. same-address refinement stability check

## Outputs

Produce:

- topology class matrix vs address difference
- resonance alignment panel
- short classification note

Target note:

`Stage_C0_11_Harmonic_Address_Resonance_Ladder_v1.md`
